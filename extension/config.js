// Default config. When you download this extension from the CRM's Extension page,
// this file is rewritten with your CRM URL + key so it works with no setup.
// For dev-from-source, leave as-is and set the URL/key via the popup.
var KNOX_CFG = { url: 'http://localhost:3001', key: '' }
